<form method="post" action="{{ path('app_tipo_bandeja_delete', {'id': tipo_bandeja.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_bandeja.id) }}">
    <button class="btn">Delete</button>
</form>
