<!DOCTYPE html>
<html lang="es">

<head>
    <style type="text/css">
        .table-border {
            border: 2px solid black;
            border-radius: 5px;
            margin-bottom: 10px;
        }
        .datos-productos-table{
            line-height: 7.5mm;
        }
        td{
            font-size: 15px;
        }
    </style>
    <title>{{ tipo_pdf }}</title>
</head>

<body>
    {% include 'PDF/header.html.twig' %}

    <div class="table-border header">
        <table width="100%" border="0">
            <tr>
                <td>
                    <table cellpadding="5" cellspacing="5" style="padding: 0;">
                        <tr>
                            <td align="right"><strong>NOMBRE</strong></td>
                            <td>{{ entity.remito.cliente.nombreCompleto|default('-') }}</td>
                            <td align="right"><strong>CUIT</strong></td>
                            <td>{{ entity.remito.cliente.cuit|default('-') }}</td>
                        </tr>
                        <tr>
                            <td align="right"><strong>CELULAR</strong></td>
                            <td>{{ entity.remito.cliente.celular|default('-') }}</td>
                            <td align="right"><strong>DOMICILIO</strong></td>
                            <td>{{ entity.remito.cliente.domicilio|default('-') }}</td>
                        </tr>
                    </table>
                </td>
            </tr>
        </table>
    </div>

    <div class="datos-productos">
        <table class="datos-productos-table" border="1" cellpadding="0" cellspacing="0" align="center">
            <thead>
            <tr>
                <th class="text-center">FECHA</th>
                <th class="text-center">REMITO</th>
                <th class="text-center">MONTO</th>
                <th class="text-center">MODO PAGO</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td width="160" align="center">{{ entity.fechaCreacion is not null ? entity.fechaCreacion|date('d/m/Y') : '-' }}</td>
                <td width="230" align="center">{{ entity.remito }}</td>
                <td width="230" align="center">{{ entity.monto|money_format  }}</td>
                <td width="160" align="center">{{ entity.modoPago  }}</td>
            </tr>
            </tbody>
            <tfoot>

            </tfoot>
        </table>
    </div>
</div>
</body>
</html>