{% extends 'base.html.twig' %}

{% block title %}Pedido{% endblock %}

{% block body %}
    <h1>Pedido</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>FechaCreacion</th>
                <td>{{ entity.fechaCreacion ? entity.fechaCreacion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>FechaUltimaModificacion</th>
                <td>{{ entity.fechaUltimaModificacion ? entity.fechaUltimaModificacion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>FechaBaja</th>
                <td>{{ entity.fechaBaja ? entity.fechaBaja|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pedido_index') }}">back to list</a>

    <a href="{{ path('pedido_edit', {'id': entity.id}) }}">edit</a>

    {{ include('pedido/_delete_form.html.twig') }}
{% endblock %}
